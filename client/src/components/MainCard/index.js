import React from "react";
import Hero from "../Hero"

const MainCard = () => {
  return (
    <div>
        <section className="card" id="mainCard">
          <Hero />
        </section>
    </div>
  );
};

export default MainCard;